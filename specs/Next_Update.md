本次更新：

1.Daily Track（不含往期推荐卡片）卡片，背景应当选用今天推荐曲目的封面，稍微模糊处理。由于卡片是长条形的，而封面是正方形的，为了保证美观，你不要对其拉伸，只需要适当放大，占到50%-60%左右，然后晕影平滑过渡。

2\.实现功能，统计当前在线人数（网站），并实现一个统计图（曲线图，自0点开始至24点，默认使用服务器时间，时区不要写死），放在Daily Track全宽卡片的正上方，并显示当前在线人数，当前时间对应在曲线图上需要标记。

