# b1.7 第三轮 — 前端 UI 返工报告 🦇🔥

> **日期**: 2026-05-03
> **测试人**: 主理人 💖 亲测
> **测试环境**: 生产环境 `https://www.purebeat.top`
> **归属**: 凛月 🎧（全部前端项）
> **状态**: 🔴 待返工

---

## 📌 一、主页 (Homepage /osu)

### 🐛 H1. FaGames 图标 + osu! 文字 → 改用户头像 + 昵称
**位置**: `/osu` 首页顶部导航区域
**现状**: 显示 FaGames 图标 + "osu!" 文字
**要求**: 替换为已登录用户的头像 + 昵称
**严重度**: P0

### 🐛 H2. 国家/地区标识未实现
**位置**: `/osu` 用户信息区域
**现状**: 未显示国家/地区标识
**要求**: 按设计显示国旗或国家代码
**严重度**: P1

### 🐛 H3. 字体仍为 Quicksand → 需换 Torus
**位置**: 全局 /osu 页面
**现状**: 使用的仍然是 Quicksand 字体
**要求**: 全部替换为 Torus 字体（osu! 标准字体）
**严重度**: P2

### 🐛 H4. 全球排名趋势图渲染两次
**位置**: 用户档案卡 → 全球排名趋势图
**现状**: 趋势图被渲染两次（重复）
**要求**: 只渲染一次，修复 React 重渲染问题
**严重度**: P2

### 🐛 H5. 星数（Star Rating）未按要求添加
**位置**: 谱面卡片/列表
**现状**: 缺少星数显示
**要求**: 按要求在对应位置加入星数
**严重度**: P0

### 🐛 H6. OD 值仍为 0
**位置**: 谱面详情/成绩详情
**现状**: OD 值始终显示为 0
**要求**: 正确显示来自 osu! API 的 OD 值（`beatmap.od`）
**严重度**: P1

---

## 📌 二、BP 排行页 (Best Performance)

### 🐛 B1. 背景渲染问题
**位置**: BP 排行页面每条卡片背景
**现状**: 纯色太突兀
**要求**: 使用不明显、柔和的背景，内容居中对齐更好看
**严重度**: P2

### 🐛 B2. 难度名被截断
**位置**: BP 排行每条的难度名显示
**现状**: `[4K] Harbinger of Death` → 被截断为 `[4K] Harbinger of D...`
**要求**: 不要截断难度名，使用合适显示方案（换行/省略在合理位置/tooltip）
**严重度**: P1

---

## 📌 三、排行榜页 (Leaderboard)

### 🐛 L1. 点击用户名跳转路径错误
**位置**: 排行榜每条记录的用户名链接
**现状**: 点击用户 → 跳转到 `/osu?user=xxx`
**要求**: 应跳转到 `/osu/info` 页面查询用户资料（因未绑定 PureBeat 账号，无法查看本地资料）
**严重度**: P0

### 🐛 L2. 用户框渲染曲绘而非用户自己的 Banner
**位置**: 排行榜每条记录的用户头像框背景
**现状**: 渲染的是当前查询谱面的曲绘
**要求**: 每个用户框应渲染该用户自己的 Banner（来源：osu! API user.cover.url）
**严重度**: P0

### 🐛 L3. 榜顶曲目卡片背景 → 曲绘不渲染
**位置**: 排行榜顶部的曲目信息卡片
**现状**: 曲绘（cover）未显示
**要求**: 背景应为谱面曲绘渲染
**严重度**: P0

### 🐛 L4. 谱面信息用图标+数值表示
**位置**: 排行榜顶部的曲目信息区域
**现状**: 纯文字
**要求**: 使用图标+数值表示：⏱ 时长 🔊 BPM ○ 圆圈总数 △ 滑条总数
**严重度**: P1

### 🐛 L5. 进度条未实现
**位置**: 排行榜顶部的统计信息区域
**现状**: 缺少进度条
**要求**: 按设计增加进度条
**严重度**: P2

### 🐛 L6. 星数（Star Rating）未显示
**位置**: 排行榜顶部的曲目卡片
**现状**: 缺少星数显示
**要求**: 显示谱面的星数评价值
**严重度**: P1

### 🐛 L7. 模式用图标+全称显示
**位置**: 排行榜顶部的曲目卡片
**现状**: 可能只显示简写
**要求**: 使用模式图标 + 模式全称（如 🎯 osu!standard / 🥁 Taiko / 🍎 Catch / ⌨️ Mania）
**严重度**: P1

---

## 📌 四、成绩详情 (Score Detail)

### 🐛 D1. Mania 判定顺序错误 & 多一档
**位置**: 成绩详情页 → 判定详情区
**现状**:
- 显示了 7 档（多一档应为 6 档）
- Perfect 放在最后
**要求**: Mania 正确顺序为 **Perf | Great | Good | Ok | Meh | Miss**（6 档，Perf 在最前）
**严重度**: P0

### 🐛 D2. 评级文字和评级图未对齐
**位置**: 成绩详情页 → 评级显示区
**现状**: 评级文字（如 S/SS）和视觉评级标记错位
**要求**: 对齐评级文字和视觉评级
**严重度**: P2

### 🐛 D3. 分数全变成 0
**位置**: 成绩详情页 → 得分
**现状**: 所有成绩的分数显示为 0
**要求**: 检查分数字段映射，确保正确显示 `score` 值
**严重度**: P0

### 🐛 D4. ACC 保留14位小数
**位置**: 成绩详情页 → 准确率
**现状**: 显示 `99.95889847924373%`（14位小数）
**要求**: 保留 2 位小数 → `99.96%`
**严重度**: P2

---

## 📌 五、同步 (Sync)

### 🐛 S1. 四模式同步仍然提示「客户端未授权」
**位置**: OAuth 同步流程
**现状**: 调用 sync-all / sync-osu 时仍然返回 401/「客户端未授权」
**要求**: 对照 osu! API v2 官方文档修复 OAuth scope / token / refresh 流程
**严重度**: P0

---

## 📊 汇总

| 区域 | 数量 | P0 | P1 | P2 |
|:----:|:----:|:--:|:--:|:--:|
| 🏠 主页 | 6 | 2 | 2 | 2 |
| 🎵 BP 排行 | 2 | 0 | 1 | 1 |
| 🏆 排行榜 | 7 | 3 | 3 | 1 |
| 📄 成绩详情 | 4 | 2 | 0 | 2 |
| 🔄 同步 | 1 | 1 | 0 | 0 |
| **合计** | **20** | **8** | **6** | **6** |

### 🔴 阻塞级 (P0) — 8 项
| # | 简述 |
|:-:|:-----|
| H1 | FaGames 图标→换用户头像+昵称 |
| H5 | 星数未添加 |
| L1 | 用户名点出跳错路径 |
| L2 | 用户框渲染曲绘而非用户 Banner |
| L3 | 榜顶曲绘未渲染 |
| D1 | Mania 判定顺序错(7档→6档) |
| D3 | 分数全变0 |
| S1 | 同步仍报客户端未授权 |

### 🟡 重要级 (P1) — 6 项
H2 国籍标识 / H6 OD=0 / B2 难度名截断 / L4 图标+数值 / L6 星数 / L7 模式图标+全称

### 🟢 改善级 (P2) — 6 项
H3 字体 / H4 趋势图×2 / B1 背景 / L5 进度条 / D2 评级对齐 / D4 ACC 小数位

---

_🧹 报告由夕奏整理—全是凛月的锅 🦇⚡️ 凛月醒醒！20个问题等着你！_
